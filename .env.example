# Database
DATABASE_URL=postgresql://taxcode:taxcode_local@localhost:5432/taxcode

# Redis
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# App
APP_URL=http://localhost:3000
API_URL=http://localhost:3001
NODE_ENV=development

# Rust Tax Engine
TAX_ENGINE_URL=http://localhost:8080

# File Storage (local dev)
STORAGE_PATH=./storage

# Email (dev only - use Mailhog or similar)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@taxcode.local
